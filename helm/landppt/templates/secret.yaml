apiVersion: v1
kind: Secret
metadata:
  name: {{ include "landppt.fullname" . }}
  labels:
    {{- include "landppt.labels" . | nindent 4 }}
type: Opaque
stringData:
  DATABASE_URL: {{ include "landppt.databaseUrl" . | quote }}
  {{- if not .Values.app.existingSecret }}
  {{- if not (index .Values.app.secrets "SECRET_KEY") }}
  {{- fail "app.existingSecret or app.secrets.SECRET_KEY must provide SECRET_KEY" }}
  {{- end }}
  {{- range $key, $value := .Values.app.secrets }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
  {{- end }}
