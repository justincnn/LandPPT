apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "landppt.fullname" . }}
  labels:
    {{- include "landppt.labels" . | nindent 4 }}
data:
  HOST: "0.0.0.0"
  PORT: {{ .Values.service.port | quote }}
  WORKERS: {{ .Values.app.workers | quote }}
  RELOAD: "false"
  LOG_LEVEL: {{ .Values.app.logLevel | quote }}
  PYTHONUNBUFFERED: "1"
  CACHE_BACKEND: "valkey"
  VALKEY_URL: {{ include "landppt.valkeyUrl" . | quote }}
  {{- range $key, $value := .Values.app.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
